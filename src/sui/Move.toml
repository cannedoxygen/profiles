[package]
name = "TardinatorProfile"
version = "1.0.0"
authors = ["TardinatorTeam"]
license = "Apache-2.0"
# These will be populated after deployment
# published-at = "0x..." # mainnet
# published-at = "0x..." # testnet

[dependencies]
Sui = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework/packages/sui-framework", rev = "mainnet" }
# Alternatively, you can use a local path if you have the Sui framework locally
# Sui = { local = "../../sui/crates/sui-framework/packages/sui-framework" }

[addresses]
tardinator_profile = "0x0" # This will be updated with the actual address after deployment
# tardinator_profile = "0x..." # mainnet
# tardinator_profile = "0x..." # testnet